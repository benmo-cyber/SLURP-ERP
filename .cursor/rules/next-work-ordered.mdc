---
description: Ordered next work after inventory pack/SO fixes
alwaysApply: true
---

# Next work (ordered)

Work **top item first**. After each ships or is confirmed, mark done / remove from this list.

## Done
1. ~~Test multi-recipe path~~ — user confirmed (dual-strength / alternate recipes).
2. ~~Rework smoke~~ — open ticket → allocate → close → on-hold output awaiting micro.
3. ~~Shelf life extension smoke~~ — campaign 2639D1307 SLE (v1→v2, member trickle) + lot 12600110 lot-level SLE; verified clean. Soft warn on second extension still in UI.
4. ~~RMA smoke~~ — open RMA → check-in → FG hold staging; credit memo clarity (`{invoice}-CM###`, PDF/register). Accept/scrap path still available if not walked end-to-end.

## Next
### 1. Finish remaining ops smoke-tests
1. **Lot mass-balance** — Quality → Lot Tracking search → production/SO/PO/RMA/lab/ledger; green/red balance.

### 2. Redesign Cost Master
Finance → Costing → Cost Master UX/structure redesign (separate from what-if freight/tariff scrubbers unless scoped together).

Touch points to discover at start: Cost Master models/UI under `slurp_ui` finance/costing and `erp_core` cost master fields.

## After / parallel (do not jump ahead of 1–2)
- What-if freight & tariff adjusters (see `whatif-freight-tariff-next.mdc`) — before other new Finance/what-if work once Cost Master redesign allows it, or immediately after Cost Master if redesign unblocks it.
- Training manual notes: COA / micro catalog (`training-manual-coa-catalog.mdc`); shelf life extension math (`training-manual-shelf-life.mdc`).

## Last (post-launch readiness)
- **Live data cutover** — plan how to load real plant data after go-live: physical inventory count + supporting entry paths (open POs/SOs, lots/COAs, customers/vendors, costs, etc.). Capture gaps vs current check-in / create flows before launch week.